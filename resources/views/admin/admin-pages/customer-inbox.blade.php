@extends('admin.admin-master')
@section('admin_main_content')
<div id="content" class="span10"><ul class="breadcrumb"><li><i class="icon-home"></i> Admin <i class="icon-angle-right"></i></li><li>Customer Inbox</li></ul>@if(session('message'))<div class="alert alert-success">{{ session('message') }}</div>@endif
<div class="row-fluid sortable"><div class="box span12"><div class="box-header"><h2><i class="halflings-icon star"></i><span class="break"></span>Product Reviews</h2></div><div class="box-content"><table class="table table-striped table-bordered"><thead><tr><th>Product / Customer</th><th>Rating</th><th>Review</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($reviews as $review)<tr><td><strong>{{ $review->product_name }}</strong><br>{{ $review->customer_name }}<br><small>{{ $review->email }}</small></td><td>{{ $review->rating }}/5</td><td>{{ $review->review }}</td><td>{{ $review->is_approved?'Published':'Pending' }}</td><td><form method="post" action="{{ url('/review/'.$review->id.'/moderate') }}">{{ csrf_field() }}<button class="btn btn-mini btn-success" name="action" value="approve">Approve</button><button class="btn btn-mini" name="action" value="hide">Hide</button><button class="btn btn-mini btn-danger" name="action" value="delete" onclick="return confirm('Delete review?')">Delete</button></form></td></tr>@empty<tr><td colspan="5">No reviews submitted.</td></tr>@endforelse</tbody></table></div></div></div>
<div class="row-fluid sortable"><div class="box span12"><div class="box-header"><h2><i class="halflings-icon question-sign"></i><span class="break"></span>Product Questions</h2></div><div class="box-content"><table class="table table-striped table-bordered"><thead><tr><th>Product / Customer</th><th>Question</th><th>Answer and Actions</th></tr></thead><tbody>@forelse($questions as $question)<tr><td><strong>{{ $question->product_name }}</strong><br>{{ $question->customer_name }}</td><td>{{ $question->question }}</td><td><form method="post" action="{{ url('/question/'.$question->id.'/answer') }}">{{ csrf_field() }}<textarea name="answer" class="span12" rows="3">{{ $question->answer }}</textarea><button class="btn btn-mini btn-primary" name="action" value="answer">Publish answer</button><button class="btn btn-mini btn-danger" name="action" value="delete" onclick="return confirm('Delete question?')">Delete</button></form></td></tr>@empty<tr><td colspan="3">No questions submitted.</td></tr>@endforelse</tbody></table></div></div></div>
<div class="row-fluid sortable"><div class="box span12"><div class="box-header"><h2><i class="halflings-icon envelope"></i><span class="break"></span>Support Requests</h2></div><div class="box-content"><table class="table table-striped table-bordered"><thead><tr><th>Customer</th><th>Request</th><th>Workflow</th></tr></thead><tbody>@forelse($supportRequests as $ticket)<tr><td><strong>{{ $ticket->customer_name }}</strong><br>{{ $ticket->email }}<br>{{ $ticket->phone }}<br><small>Order: {{ $ticket->order_number ?: '—' }}</small></td><td><strong>{{ $ticket->subject }}</strong><p>{{ $ticket->message }}</p></td><td><form method="post" action="{{ url('/support/'.$ticket->id.'/update') }}">{{ csrf_field() }}<select name="status"><option value="new" {{ $ticket->status==='new'?'selected':'' }}>New</option><option value="in_progress" {{ $ticket->status==='in_progress'?'selected':'' }}>In progress</option><option value="resolved" {{ $ticket->status==='resolved'?'selected':'' }}>Resolved</option></select><textarea name="admin_note" class="span12" rows="3" placeholder="Internal note">{{ $ticket->admin_note }}</textarea><button class="btn btn-mini btn-primary">Update</button></form></td></tr>@empty<tr><td colspan="3">No support requests.</td></tr>@endforelse</tbody></table></div></div></div>
</div>
@endsection
