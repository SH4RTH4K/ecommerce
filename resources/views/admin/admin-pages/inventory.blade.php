@extends('admin.admin-master')
@section('admin_main_content')
<div id="content" class="span10"><ul class="breadcrumb"><li><i class="icon-home"></i> Admin <i class="icon-angle-right"></i></li><li>Inventory</li></ul>@if(session('message'))<div class="alert alert-success">{{ session('message') }}</div>@endif
<div class="row-fluid"><div class="span4 statbox green"><div class="number">{{ $counts['tracked'] }}</div><div class="title">tracked products</div></div><div class="span4 statbox yellow"><div class="number">{{ $counts['low'] }}</div><div class="title">low stock</div></div><div class="span4 statbox red"><div class="number">{{ $counts['out'] }}</div><div class="title">out of stock</div></div></div>
<div class="box"><div class="box-header"><h2><i class="halflings-icon tasks"></i><span class="break"></span>Stock Control</h2></div><div class="box-content"><form class="form-inline" method="get"><input name="search" value="{{ request('search') }}" placeholder="Product or SKU"><select name="filter"><option value="">All products</option><option value="low" {{ request('filter')==='low'?'selected':'' }}>Low stock (1–5)</option><option value="out" {{ request('filter')==='out'?'selected':'' }}>Out of stock</option><option value="untracked" {{ request('filter')==='untracked'?'selected':'' }}>Not tracked</option></select><button class="btn btn-primary">Filter</button><a class="btn" href="{{ url('/inventory') }}">Reset</a></form><hr>
<table class="table table-striped table-bordered"><thead><tr><th>Product</th><th>SKU</th><th>Current state</th><th>Stock control</th></tr></thead><tbody>@foreach($products as $product)<tr><td><strong>{{ $product->product_name }}</strong><br><small>{{ $product->product_model }}</small></td><td>{{ $product->sku ?: '—' }}</td><td>@if($product->stock_tracking)<span class="label {{ $product->stock_quantity>5?'label-success':($product->stock_quantity>0?'label-warning':'label-important') }}">{{ $product->stock_quantity }} available</span>@else<span class="label">Not tracked</span>@endif</td><td><form class="form-inline" method="post" action="{{ url('/inventory/'.$product->id) }}">{{ csrf_field() }}<input type="hidden" name="return_filter" value="{{ request('filter') }}"><input type="hidden" name="return_search" value="{{ request('search') }}"><label class="checkbox"><input type="checkbox" name="stock_tracking" value="1" {{ $product->stock_tracking?'checked':'' }}> Track</label> <input type="number" min="0" name="stock_quantity" value="{{ $product->stock_quantity }}" style="width:75px"><input type="hidden" name="product_condition" value="{{ $product->product_condition }}"><button class="btn btn-mini btn-primary">Save</button></form></td></tr>@endforeach</tbody></table>{{ $products->links() }}</div></div></div>
@endsection
