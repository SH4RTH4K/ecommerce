@extends('admin.admin-master')
@section('admin_main_content')
<div id="content" class="span10"><ul class="breadcrumb"><li><i class="icon-home"></i> Admin <i class="icon-angle-right"></i></li><li>Delivery Zones</li></ul>@if(session('message'))<div class="alert alert-success">{{ session('message') }}</div>@endif @if(session('exception'))<div class="alert alert-error">{{ session('exception') }}</div>@endif @if($errors->any())<div class="alert alert-error">{{ $errors->first() }}</div>@endif
<div class="box"><div class="box-header"><h2><i class="halflings-icon road"></i><span class="break"></span>Add Delivery Zone</h2></div><div class="box-content"><form class="form-inline" method="post" action="{{ url('/delivery-zones') }}">{{ csrf_field() }}<input name="name" placeholder="Zone name" required><input name="areas" placeholder="Covered areas"><input type="number" min="0" step="0.01" name="charge" placeholder="Charge" required><input type="number" min="0" step="0.01" name="free_delivery_minimum" placeholder="Free over"><input name="estimated_time" placeholder="Estimated time"><input type="number" min="0" name="display_order" value="0" style="width:55px"><label class="checkbox"><input type="checkbox" name="is_active" value="1" checked> Active</label><button class="btn btn-primary">Add zone</button></form></div></div>
<div class="box"><div class="box-header"><h2>Configured Zones</h2></div><div class="box-content"><table class="table table-striped table-bordered"><thead><tr><th>Zone configuration</th><th>Status</th><th>Actions</th></tr></thead><tbody>@foreach($zones as $zone)<tr><td><form class="form-inline" method="post" action="{{ url('/delivery-zones') }}">{{ csrf_field() }}<input type="hidden" name="id" value="{{ $zone->id }}"><input name="name" value="{{ $zone->name }}" required><input name="areas" value="{{ $zone->areas }}"><input type="number" min="0" step="0.01" name="charge" value="{{ $zone->charge }}" style="width:75px"><input type="number" min="0" step="0.01" name="free_delivery_minimum" value="{{ $zone->free_delivery_minimum }}" placeholder="Free over" style="width:90px"><input name="estimated_time" value="{{ $zone->estimated_time }}"><input type="number" min="0" name="display_order" value="{{ $zone->display_order }}" style="width:50px"><input type="hidden" name="is_active" value="{{ $zone->is_active?1:0 }}"><button class="btn btn-mini btn-primary">Save</button></form></td><td><span class="label {{ $zone->is_active?'label-success':'' }}">{{ $zone->is_active?'Active':'Hidden' }}</span></td><td><form method="post" action="{{ url('/delivery-zones/'.$zone->id.'/toggle') }}" style="display:inline">{{ csrf_field() }}<button class="btn btn-mini">Toggle</button></form> <form method="post" action="{{ url('/delivery-zones/'.$zone->id.'/delete') }}" style="display:inline">{{ csrf_field() }}<button class="btn btn-mini btn-danger" onclick="return confirm('Delete this delivery zone?')">Delete</button></form></td></tr>@endforeach</tbody></table></div></div></div>
@endsection
