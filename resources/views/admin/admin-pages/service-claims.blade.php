@extends('admin.admin-master')
@section('admin_main_content')
<div id="content" class="span10"><ul class="breadcrumb"><li><i class="icon-home"></i> Admin <i class="icon-angle-right"></i></li><li>Warranty &amp; RMA</li></ul><div class="box"><div class="box-header"><h2><i class="icon-legal"></i> Service requests</h2></div><div class="box-content"><form class="form-inline" method="get"><input name="search" value="{{ request('search') }}" placeholder="Claim, customer, product or phone"><select name="status"><option value="">All statuses</option>@foreach(['submitted','reviewing','approved','item_received','in_service','ready','completed','rejected'] as $status)<option value="{{ $status }}" {{ request('status')===$status?'selected':'' }}>{{ ucwords(str_replace('_',' ',$status)) }}</option>@endforeach</select><button class="btn btn-primary">Filter</button></form><hr><table class="table table-striped table-bordered"><thead><tr><th>Reference</th><th>Customer</th><th>Product</th><th>Type</th><th>Status</th><th>Date</th><th></th></tr></thead><tbody>@forelse($claims as $claim)<tr><td><strong>{{ $claim->claim_number }}</strong></td><td>{{ $claim->customer_name }}<br><small>{{ $claim->phone }}</small></td><td>{{ $claim->product_name }}</td><td>{{ ucfirst($claim->claim_type) }}</td><td><span class="label">{{ ucwords(str_replace('_',' ',$claim->status)) }}</span></td><td>{{ date('M j, Y',strtotime($claim->created_at)) }}</td><td><a class="btn btn-mini btn-primary" href="{{ url('/service-claims/'.$claim->id) }}">Manage</a></td></tr>@empty<tr><td colspan="7">No service requests found.</td></tr>@endforelse</tbody></table>{{ $claims->links() }}</div></div></div>
@endsection
