@extends('admin.admin-master')
@section('admin_main_content')
<div id="content" class="span10"><ul class="breadcrumb"><li><i class="icon-home"></i> Admin <i class="icon-angle-right"></i></li><li>Returns &amp; Refunds</li></ul><div class="box"><div class="box-header"><h2><i class="icon-undo"></i> Return requests</h2></div><div class="box-content"><form class="form-inline" method="get"><input name="search" value="{{ request('search') }}" placeholder="Return, order or customer"><select name="status"><option value="">All statuses</option>@foreach(['requested','approved','rejected','received','refunded'] as $status)<option value="{{ $status }}" {{ request('status')===$status?'selected':'' }}>{{ ucfirst($status) }}</option>@endforeach</select><button class="btn btn-primary">Filter</button></form><hr><table class="table table-striped table-bordered"><thead><tr><th>Return</th><th>Order</th><th>Customer</th><th>Amount</th><th>Status</th><th>Date</th><th></th></tr></thead><tbody>@forelse($returns as $return)<tr><td><strong>{{ $return->return_number }}</strong></td><td>{{ $return->order_number }}</td><td>{{ $return->customer_name }}<br><small>{{ $return->phone }}</small></td><td>৳{{ number_format($return->approved_amount?:$return->requested_amount,2) }}</td><td><span class="label">{{ ucfirst($return->status) }}</span></td><td>{{ date('M j, Y',strtotime($return->created_at)) }}</td><td><a class="btn btn-mini btn-primary" href="{{ url('/returns/'.$return->id) }}">Manage</a></td></tr>@empty<tr><td colspan="7">No return requests found.</td></tr>@endforelse</tbody></table>{{ $returns->links() }}</div></div></div>
@endsection
