@extends('admin.admin-master')
@section('admin_main_content')
<div id="content" class="span10"><ul class="breadcrumb"><li><a href="{{ url('/manage-orders') }}">Orders</a> <i class="icon-angle-right"></i></li><li>{{ $order->order_number }}</li></ul><p><a class="btn btn-primary" target="_blank" href="{{ route('orders.invoice',$order->id) }}"><i class="icon-print"></i> Print invoice</a></p>@if(session('message'))<div class="alert alert-success">{{ session('message') }}</div>@endif
<div class="row-fluid"><div class="box span7"><div class="box-header"><h2>Order Items</h2></div><div class="box-content"><table class="table table-bordered"><thead><tr><th>Product</th><th>SKU</th><th>Offer Price</th><th>Qty</th><th>Subtotal</th></tr></thead><tbody>@foreach($items as $item)<tr><td>{{ $item->product_name }}</td><td>{{ $item->sku ?: '—' }}</td><td>৳{{ number_format($item->offer_price) }}</td><td>{{ $item->quantity }}</td><td>৳{{ number_format($item->subtotal) }}</td></tr>@endforeach</tbody></table><dl><dt>Delivery address</dt><dd>{{ $order->address }}, {{ $order->city }}</dd><dt>Order note</dt><dd>{{ $order->notes ?: '—' }}</dd></dl></div></div>
<div class="box span5"><div class="box-header"><h2>Order {{ $order->order_number }}</h2></div><div class="box-content"><p><strong>{{ $order->customer_name }}</strong><br>{{ $order->phone }}<br>{{ $order->email }}</p><p>Subtotal: <strong>৳{{ number_format($order->subtotal) }}</strong><br>Delivery: <strong>৳{{ number_format($order->delivery_charge) }}</strong><br>Total: <strong>৳{{ number_format($order->total) }}</strong></p><form method="post" action="{{ url('/manage-orders/'.$order->id.'/status') }}">{{ csrf_field() }}<label>Status</label><select class="span12" name="status">@foreach(['pending','confirmed','processing','shipped','delivered','cancelled'] as $status)<option value="{{ $status }}" {{ $order->status===$status?'selected':'' }}>{{ ucfirst($status) }}</option>@endforeach</select><label>Delivery charge</label><input class="span12" type="number" min="0" step="0.01" name="delivery_charge" value="{{ $order->delivery_charge }}"><button class="btn btn-primary">Update order</button></form></div></div></div></div>
@endsection
