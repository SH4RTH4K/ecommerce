@extends('admin.admin-master')
@section('admin_main_content')
<div id="content" class="span10"><ul class="breadcrumb"><li><i class="icon-home"></i> Admin <i class="icon-angle-right"></i></li><li>Abandoned Carts</li></ul>@if(session('message'))<div class="alert alert-success">{{ session('message') }}</div>@endif @if(session('exception'))<div class="alert alert-error">{{ session('exception') }}</div>@endif<div class="row-fluid"><a class="quick-button span3" href="{{ url('/abandoned-carts?status=active') }}"><i class="icon-shopping-cart"></i><p>Active</p><span class="notification yellow">{{ $counts['active'] }}</span></a><a class="quick-button span3" href="{{ url('/abandoned-carts?status=reminded') }}"><i class="icon-envelope"></i><p>Reminded</p><span class="notification blue">{{ $counts['reminded'] }}</span></a><a class="quick-button span3" href="{{ url('/abandoned-carts?status=recovered') }}"><i class="icon-ok"></i><p>Recovered</p><span class="notification green">{{ $counts['recovered'] }}</span></a></div><div class="box"><div class="box-header"><h2>Cart recovery queue</h2></div><div class="box-content"><table class="table table-striped table-bordered"><thead><tr><th>Customer</th><th>Items</th><th>Value</th><th>Last activity</th><th>Status</th><th>Email</th><th>Actions</th></tr></thead><tbody>@forelse($carts as $cart)<tr><td>{{ $cart->email ?: 'Guest without email' }}</td><td>{{ array_sum((array)json_decode($cart->items,true)) }}</td><td>৳{{ number_format($cart->subtotal) }}</td><td>{{ date('M j, Y g:i A',strtotime($cart->last_activity_at)) }}</td><td><span class="label">{{ ucfirst($cart->status) }}</span></td><td>{{ str_replace('_',' ',$cart->email_status) }}</td><td>@if($cart->status==='active'&&$cart->email)<form method="post" action="{{ url('/abandoned-carts/'.$cart->id.'/remind') }}" style="display:inline">{{ csrf_field() }}<button class="btn btn-mini btn-primary">Send reminder</button></form>@endif <form method="post" action="{{ url('/abandoned-carts/'.$cart->id.'/delete') }}" style="display:inline">{{ csrf_field() }}<button class="btn btn-mini btn-danger" onclick="return confirm('Delete this saved cart?')">Delete</button></form></td></tr>@empty<tr><td colspan="7">No saved carts found.</td></tr>@endforelse</tbody></table>{{ $carts->links() }}</div></div></div>
@endsection
