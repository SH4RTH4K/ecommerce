; Admin image uploads are limited to 8 MB by the application.
; Keep PHP request limits higher so one or more card images can be validated.
upload_max_filesize=10M
post_max_size=20M
max_file_uploads=20
max_input_time=120
max_execution_time=120
memory_limit=256M
